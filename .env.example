# Copy to `.env` and fill in values from Supabase Dashboard → Project Settings → API.
# Do NOT commit `.env`. The publishable key is safe for client embedding but
# still not worth versioning; service_role keys must NEVER go here.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=sb_publishable_xxx
